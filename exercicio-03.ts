//Exercício 03: Declare uma variável chamada "númeroMáximo", 
//atribua a ela o valor máximo que um número pode ter em JavaScript e imprima o valor na console.

const numeroMaximo: number = Number.MAX_SAFE_INTEGER

alert (`Curiosidade do Dia!

Você sabia que o maior valor inteiro seguro no JavaScript/Typescript é utilizando o Number.MAX_SAFE_INTEGER ?

Ao rodar esta constante você obterá o seguinte valor: 
                                
                            ${numeroMaximo}`)